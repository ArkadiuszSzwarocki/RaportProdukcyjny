02-drop-szarze_parametry.sql — runs DROP TABLE IF EXISTS szarze_parametry;

To run it locally you can use the helper script in `scripts/drop_szarze_parametry.sh`.
Set environment variables: DB_HOST, DB_USER, DB_PASSWORD, DB_NAME
Always backup your DB before running.
