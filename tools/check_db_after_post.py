"""
check_db_after_post.py — disabled

This helper printed DB rows for quick debugging. Disabled to avoid accidental
DB reads during normal workflows. Restore from git history to use again.
"""
import sys

def main():
    print('tools/check_db_after_post.py is disabled. Restore from git if needed.')

if __name__ == '__main__':
    main()
